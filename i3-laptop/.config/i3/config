# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see https://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4


###############################################################################
# Keycodes (physical keys) from xev
###############################################################################

# Top row: qwfpbkluy;[]\  qwertyuiop[]\
set $kc_q_q 24
set $kc_w_w 25
set $kc_f_e 26
set $kc_p_r 27
set $kc_b_t 28
set $kc_k_y 29
set $kc_l_u 30
set $kc_u_i 31
set $kc_y_o 32
set $kc_semicolon_p 33
set $kc_lbracket 34
set $kc_rbracket 35
set $kc_bslash 51

# Home row: arstgmneio'
set $kc_a_a 38
set $kc_r_s 39
set $kc_s_d 40
set $kc_t_f 41
set $kc_g_g 42
set $kc_m_h 43
set $kc_n_j 44
set $kc_e_k 45
set $kc_i_l 46
set $kc_o_semicolon 47
set $kc_apostrophe 48

# Bottom row (partial, from your dump)
set $kc_v_z 52
set $kc_x_x 53
set $kc_c_c 54
set $kc_d_v 55
set $kc_z_b 56
set $kc_j_n 57
set $kc_h_m 58

# Space / Mod helpers
set $kc_space 65


# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
font pango:monospace 14

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
#font pango:DejaVu Sans Mono 8


exec --no-startup-id xrandr --output eDP-1 --scale 1.25x1.25
# Wallpaper
exec --no-startup-id feh --bg-scale /home/bvr/Pictures/wallpaper/Linux/ubuntu-wallpapers-15.jpg

# Start XDG autostart .desktop files using dex. See also
# https://wiki.archlinux.org/index.php/XDG_Autostart
exec --no-startup-id dex --autostart --environment i3

# The combination of xss-lock, nm-applet and pactl is a popular choice, so
# they are included here as an example. Modify as you see fit.

# HiDPI scaling
# exec --no-startup-id xrdb -merge <<< "Xft.dpi: 168"

exec_always --no-startup-id xrdb -merge ~/.Xresources

exec --no-startup-id picom --backend glx --vsync

# xss-lock grabs a logind suspend inhibit lock and will use i3lock to lock the
# screen before suspend. Use loginctl lock-session to lock your screen.
# exec --no-startup-id xss-lock --transfer-sleep-lock -- i3lock --nofork
exec --no-startup-id xss-lock --transfer-sleep-lock -- i3lock -c 000000

# NetworkManager is the most popular way to manage wireless networks on Linux,
# and nm-applet is a desktop environment-independent system tray GUI for it.
exec --no-startup-id nm-applet

exec_always --no-startup-id xbanish

# Use pactl to adjust volume in PulseAudio.
set $refresh_i3status killall -SIGUSR1 i3status
bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +10% && $refresh_i3status
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -10% && $refresh_i3status
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle && $refresh_i3status
bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle && $refresh_i3status


# Proton startup
exec --no-startup-id proton-startup.sh

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# move tiling windows via drag & drop by left-clicking into the title bar,
# or left-clicking anywhere into the window while holding the floating modifier.
tiling_drag modifier titlebar

# start a terminal
bindsym $mod+Return exec i3-sensible-terminal

# kill focused window
bindcode $mod+Shift+$kc_q_q kill

# start dmenu (a program launcher)
# bindsym $mod+space exec --no-startup-id dmenu_run
bindsym $mod+space exec rofi -show drun
# A more modern dmenu replacement is rofi:
# bindcode $mod+40 exec "rofi -modi drun,run -show drun"
# There also is i3-dmenu-desktop which only displays applications shipping a
# .desktop file. It is a wrapper around dmenu, so you need that installed.
# bindcode $mod+40 exec --no-startup-id i3-dmenu-desktop

# toggle colmak qwerty
bindcode $mod+Shift+59 exec --no-startup-id ~/.local/bin/kb-toggle.sh

# Power controls
bindsym $mod+Shift+Escape exec systemctl poweroff
bindsym $mod+Shift+BackSpace exec systemctl reboot
bindsym $mod+Shift+Delete exec i3lock

# change focus
bindcode $mod+$kc_h_m focus left
bindcode $mod+$kc_j_n focus down
bindcode $mod+$kc_k_y focus up
bindcode $mod+$kc_l_u focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindcode $mod+Shift+$kc_h_m move left
bindcode $mod+Shift+$kc_j_n move down
bindcode $mod+Shift+$kc_k_y move up
bindcode $mod+Shift+$kc_l_u move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindcode $mod+$kc_b_t split h

# split in vertical orientation
bindcode $mod+$kc_v_z split v

# enter fullscreen mode for the focused container
bindcode $mod+$kc_f_e fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindcode $mod+$kc_s_d layout stacking
bindcode $mod+$kc_t_f layout tabbed
bindcode $mod+$kc_a_a layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindcode $mod+$kc_n_j    focus mode_toggle

# focus the parent container
# bindcode $mod+a focus parent

# focus the child container
#bindcode $mod+d focus child

# shortcuts
bindcode $mod+Shift+$kc_n_j exec nautilus
# bindsym Control+Shift+braceright exec brave-browser
bindsym Control+Shift+braceright exec brave-browser-hidpi
# bindsym Control+Shift+braceleft exec google-chrome
bindsym Control+Shift+braceleft exec google-chrome-hidpi
bindsym Control+Shift+apostrophe exec code
bindsym Print exec flameshot gui

# Define names for default workspaces for which we configure key bindings later on.
# We use variables to avoid repeating the names in multiple places.
set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10"

# switch to workspace
bindsym $mod+1 workspace number $ws1
bindsym $mod+2 workspace number $ws2
bindsym $mod+3 workspace number $ws3
bindsym $mod+4 workspace number $ws4
bindsym $mod+5 workspace number $ws5
bindsym $mod+6 workspace number $ws6
bindsym $mod+7 workspace number $ws7
bindsym $mod+8 workspace number $ws8
bindsym $mod+9 workspace number $ws9
bindsym $mod+0 workspace number $ws10

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace number $ws1
bindsym $mod+Shift+2 move container to workspace number $ws2
bindsym $mod+Shift+3 move container to workspace number $ws3
bindsym $mod+Shift+4 move container to workspace number $ws4
bindsym $mod+Shift+5 move container to workspace number $ws5
bindsym $mod+Shift+6 move container to workspace number $ws6
bindsym $mod+Shift+7 move container to workspace number $ws7
bindsym $mod+Shift+8 move container to workspace number $ws8
bindsym $mod+Shift+9 move container to workspace number $ws9
bindsym $mod+Shift+0 move container to workspace number $ws10

# reload the configuration file
bindcode $mod+Shift+$kc_r_s reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindcode $mod+Shift+$kc_p_r restart
# exit i3 (logs you out of your X session)
# bindcode $mod+Shift+F exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -B 'Yes, exit i3' 'i3-msg exit'"

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindcode $kc_h_m resize shrink width 10 px or 10 ppt
        bindcode $kc_j_n resize grow height 10 px or 10 ppt
        bindcode $kc_k_y resize shrink height 10 px or 10 ppt
        bindcode $kc_l_u resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape or $mod+r
        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindcode $mod+$kc_p_r mode "default"
}

bindcode $mod+$kc_p_r mode "resize"

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar {
    position top
    font pango:monospace 18
    # status_command i3status
    status_command i3status --config ~/.config/i3status/config
} 



###############################################################################
# Window 
###############################################################################

# 1 Browser
for_window [class="Google-chrome"] move to workspace $ws1, layout tabbed
# for_window [class="Vivaldi-stable"] move to workspace $ws1, layout tabbed
for_window [class="Brave-browser"] move to workspace $ws1, layout tabbed
for_window [class="proton mail"] move to workspace $ws1, layout tabbed
for_window [class="Proton Mail"] move to workspace $ws1, layout tabbed
# org.gnome.Nautilus

# 2 Terminal
for_window [class="Alacritty"] move to workspace $ws2, layout tabbed
for_window [class="org.remmina.Remmina"] move to workspace $ws2, layout tabbed

# 3 IDE
for_window [class="Code"] move to workspace $ws3, layout tabbed
for_window [class="MPLAB X IDE v6.25"] move to workspace $ws3, layout tabbed
for_window [class="rstudio"] move to workspace $ws3, layout tabbed
for_window [class="kicad"] move to workspace $ws3, layout tabbed
for_window [class="ondel-es"] move to workspace $ws3, layout tabbed

# 4 Program
for_window [class="com-st-microxplorer-maingui-STM32CubeMX"] move to workspace $ws4, layout tabbed
for_window [class="com.st.app.Main"] move to workspace $ws4, layout tabbed
for_window [class="MPLAB IPE v6.25"] move to workspace $ws4, layout tabbed
for_window [class="bambu-studio"] move to workspace $ws4, layout tabbed

# 5 Debug
for_window [class="Ozone"] move to workspace $ws5, layout tabbed
for_window [class="SystemView"] move to workspace $ws5, layout tabbed
for_window [class="tabby"] move to workspace $ws5, layout tabbed
for_window [class="logic"] move to workspace $ws5, layout tabbed

# 6 Network
for_window [class="MQTT-Explorer"] move to workspace $ws6, layout tabbed
for_window [class="SerialTest"] move to workspace $ws6, layout tabbed
for_window [class="mongodb compass"] move to workspace $ws6, layout tabbed
for_window [class="Angry IP Scanner"] move to workspace $ws6, layout tabbed
for_window [class="Wireshark"] move to workspace $ws6, layout tabbed

# 7 Graphic
for_window [class="draw.io"] move to workspace $ws7, layout tabbed
for_window [class="fadein"] move to workspace $ws7, layout tabbed
for_window [class="obsidian"] move to workspace $ws7, layout tabbed
for_window [class="gimp-2.10"] move to workspace $ws7, layout tabbed
for_window [class="Inkscape"] move to workspace $ws7, layout tabbed
for_window [class="masterpdfeditor5"] move to workspace $ws7, layout tabbed
for_window [class="DesktopEditors"] move to workspace $ws7, layout tabbed
for_window [class="Gnome-terminal"] move to workspace $ws7, layout tabbed

# 8 Media
for_window [class="vlc"] move to workspace $ws8, layout tabbed
for_window [class="obs"] move to workspace $ws8, layout tabbed

# 9
#for_window [class="outlook-for-linux"] move to workspace $ws9, layout tabbed

# 10 Util
for_window [class="stacer"] move to workspace $ws10, layout tabbed
for_window [class="NordPass"] move to workspace $ws10, layout tabbed
for_window [class="Insync"] move to workspace $ws10, layout tabbed
for_window [class="Vial"] move to workspace $ws10, layout tabbed
for_window [class="gnome-disks"] move to workspace $ws10, layout tabbed
for_window [class="input-remapper-gtk"] move to workspace $ws10, layout tabbed
for_window [class="protonvpn-app"] move to workspace $ws10, layout tabbed
for_window [class="Protonvpn-app"] move to workspace $ws10, layout tabbed
for_window [class="Proton-authenticator"] move to workspace $ws10, layout tabbed
for_window [class="proton-authenticator"] move to workspace $ws10, layout tabbed
for_window [class="Proton Pass"] move to workspace $ws10, layout tabbed
for_window [class="proton pass"] move to workspace $ws10, layout tabbed


